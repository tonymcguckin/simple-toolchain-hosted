version: '2'
messages:
  #$i18n: locales.yml
template:
  name: foo
    #$ref: "$env.tname !== undefined ? '#/messages/template.name1' : '#/messages/template.name2'"
  #description:
    #$ref: "#/messages/template.description"
toolchain:
  name: "foo"
  #template:
    #getting_started:
    #  $ref: "#/messages/template.description"

  #config:
   # repo_name: $env.sample-repo.parameters.repo_name

services:
  repo:
    $switch:
      - $expr: default
      - $default: default
        service_id: orion
        parameters:
          test: test

#form:
#  pipeline:
#    parameters: {}
#    schema:
#      $ref: "$env.d === undefined ? 'deploy.json' : 'deploy-dtype.json'"
