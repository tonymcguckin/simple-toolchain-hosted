version: '2'
messages:
  $i18n: locales.yml
template:
  name: "Simple BYOC c/w Dynamic Source Providers"
  description: "Simple BYOC c/w Dynamic Source Providers"
  header: '![](toolchain.svg?localize)'
  icon: icon.svg
  required:
    - sample-build
    - sample-repo
  info:
    git url: >-
      [https://github.com/tonymcguckin/simple-toolchain-hosted](https://github.com/tonymcguckin/simple-toolchain-hosted)
    git branch: >-
      [master](https://github.com/tonymcguckin/simple-toolchain-hosted/tree/master)
toolchain:
  name: 'Simple BYOC c/w Dynamic Source Providers'
  template:
    getting_started:
      $ref: "#/messages/template.gettingStarted"
services:
  sample-repo:
    $default: 'hostedgit'
    $options:
      - $ifdef: 'hostedgit'
        service_id: hostedgit
        parameters:
          repo_name: 'BYOC MyApp'
          repo_url: 'https://git.stage1.ng.bluemix.com/myrepo/byoc-myapp-onhostedgit'
          type: 'link'
          has_issues: true
          enable_traceability: true
      - $ifdef: 'githubpublic'
        service_id: githubpublic
        parameters:
          repo_name: 'BYOC MyApp'
          repo_url: 'https://github.com/myrepo/byoc-myapp-ongithubpublic'
          type: 'link'
          visibility: public
      - $ifdef: 'github_integrated'
        service_id: github_integrated
        parameters:
          repo_name: 'BYOC MyApp'
          repo_url: 'https://github.stage1.ng.bluemix.com/myrepo/myapp-ongithub_integrated'
          type: 'link'
          has_issues: true
          enable_traceability: true
  sample-build:
    service_id: pipeline
    parameters:
      services:
        - sample-repo
      name: '{{services.sample-repo.parameters.repo_name}}'
      ui-pipeline: true
      configuration:
        content:
          $text: pipeline.yml
        env:
          SAMPLE_REPO: sample-repo
          CF_APP_NAME: '{{form.pipeline.parameters.prod-app-name}}'
          PROD_SPACE_NAME: '{{form.pipeline.parameters.prod-space}}'
          PROD_ORG_NAME: '{{form.pipeline.parameters.prod-organization}}'
          PROD_REGION_ID: '{{form.pipeline.parameters.prod-region}}'
          API_KEY: '{{form.pipeline.parameters.api-key}}'
        execute: true
  webide:
    service_id: orion
form:
  pipeline:
    parameters:
      prod-app-name: '{{services.sample-repo.parameters.repo_name}}'
    schema:
      $ref: deploy.json
