version: '2'

messages:
  $i18n: locales.yml

template:
  name:
    $ref: "#/messages/template.name"
    #$ref: "$env.tname !== undefined ? #/messages/template.name : #/messages/template.name"

  #description:
    #$ref: "#/messages/template.description"

toolchain:
  name: "foo"

#template:
    #getting_started:
    #  $ref: "#/messages/template.description"

  #config:
   # repo_name: $env.sample-repo.parameters.repo_name

services:
  repo:
    $switch: !!pairs
      - $expr: default
      - $default: default
        service_id: orion
        parameters:
          test: test

form:
  pipeline:
    parameters: {}
    schema:
      $ref: deploy.json
